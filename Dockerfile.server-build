FROM debian:12

RUN apt-get update && apt-get install -y \
    build-essential \
    cmake \
    git \
    python3 \
    libcurl4-openssl-dev \
    libssl-dev \
    libsqlite3-dev \
    zlib1g-dev \
    rustc \
    cargo \
    pkg-config \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /build
